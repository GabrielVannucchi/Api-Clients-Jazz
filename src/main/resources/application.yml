server:
  error:
    include-stacktrace: ON_PARAM;

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/clients?useSSL=false&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update